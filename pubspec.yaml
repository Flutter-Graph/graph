name: graph
description: Graph representation on top of isar
version: 0.0.1
author: null
homepage: null

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.24.0"

dependencies:
  flutter:
    sdk: flutter
  linalg: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: null
